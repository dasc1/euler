defmodule X do
    
    def ans do
        a = 2 * 3 * 5 * 7 * 11 * 13 * 17 * 19 # primes
        b = a * 2 * 3 # prime squares
        c = b * 2 # prime cubes
        d = c * 2 # prime quads
        d
    end

end
